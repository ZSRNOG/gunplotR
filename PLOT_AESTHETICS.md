# gunplotR 图形美化参数指南

这个文件说明如何用更接近 R 基础绘图函数 `plot()` 和 `ggplot2` 的方式美化
`gunplotR` 图形。包仍然调用外部 `gunplot` / `gnuplot` 绘图，但常用参数可以直接
从 R 里传入。

## 基础原则

- `output = NULL` 是默认值，因此默认只弹出窗口预览，不保存图片。
- 只有显式设置 `output = "xxx.png"`、`"xxx.pdf"` 等路径时才会保存。
- 颜色参数支持数字，例如 `col = 2` 会使用 R 当前调色板里的第 2 个颜色。
- 颜色参数也支持常见颜色名和十六进制值，例如 `col = "red"` 或
  `col = "#3366cc"`。
- 复杂图推荐用 `gp_layer()` + `gp_multi()` 分层；每一层可以单独设置颜色、线型、
  点形、线宽等。
- gnuplot 6 的通用 `set` 命令可以用 `settings = gp_options(...)` 或
  `extra = gp_options(...)` 映射为 R 风格参数。

## 常用美化参数

| 参数 | 含义 | 示例 |
| --- | --- | --- |
| `col`, `color` | 线和点的颜色 | `col = 2`, `col = "#3366cc"` |
| `line_col` | 线颜色别名 | `line_col = 4` |
| `point_col` | 点颜色别名 | `point_col = "darkred"` |
| `lwd` | 线宽 | `lwd = 2` |
| `lty` | 线型 / dash type | `lty = 2` |
| `pch` | 点形 / point type | `pch = 7` |
| `cex` | 点大小 / point size | `cex = 1.4` |
| `main` | 主标题 | `main = "Sine wave"` |
| `subtitle` | 副标题 | `subtitle = "Generated by gnuplot"` |
| `legend` | 图例文字 | `legend = "sin(x)"` |
| `theme` | 整体主题 | `theme = gp_theme(...)` |

## gnuplot 6 通用设置

复杂外观可以用 `settings = gp_options(...)` 映射到 gnuplot 的 `set` 命令：

```r
gp_line(
  1:100,
  sqrt(1:100),
  settings = gp_options(
    xlog = TRUE,
    xtics = c(1, 10, 100),
    yformat = "%.2f",
    key = "outside",
    border = "3 lw 1.5",
    style_fill = "solid 0.5 border lc black"
  ),
  col = 4,
  lwd = 2
)
```

可复用线型和箭头样式：

```r
gp_function(
  c("sin(x)", "cos(x)"),
  settings = gp_options(
    linestyle = c(
      gp_linestyle(1, col = 2, lwd = 2, lty = 1),
      gp_linestyle(2, col = 4, lwd = 2, lty = 2)
    )
  ),
  col = c(2, 4),
  lwd = 2
)
```

## 单条曲线

```r
library(gunplotR)

x <- seq(0, 2 * pi, length.out = 300)

gp_line(
  x, sin(x),
  col = 2,
  lwd = 2,
  lty = 1,
  main = "Sine wave",
  subtitle = "Preview by default, no file is saved",
  xlab = "x",
  ylab = "sin(x)",
  legend = "sin(x)"
)
```

## 散点图

```r
gp_scatter(
  mtcars$wt, mtcars$mpg,
  pch = 7,
  cex = 1.3,
  col = "#3366cc",
  main = "MPG vs weight",
  xlab = "Weight",
  ylab = "MPG"
)
```

## 多条函数曲线

`gp_function()` 的 `col/lwd/lty/pch/cex` 可以传入长度为 1 的值，也可以传入和曲线
数量相同的向量。

```r
gp_function(
  c("sin(x)", "cos(x)", "sin(x) * cos(x)"),
  xrange = c(-2 * pi, 2 * pi),
  col = c(2, 4, 3),
  lwd = c(2, 2, 1.5),
  lty = c(1, 2, 3),
  main = "Multiple functions",
  subtitle = "Vectorized style parameters",
  xlab = "x",
  ylab = "f(x)"
)
```

## 多图层绘图

多图层时，每层用 `gp_layer()` 单独设置样式，整体图形用 `gp_multi()` 设置标题、
坐标轴和主题。

```r
df <- mtcars
fit <- lm(mpg ~ wt, data = df)

xline <- seq(min(df$wt), max(df$wt), length.out = 100)
trend <- data.frame(wt = xline, mpg = predict(fit, data.frame(wt = xline)))

gp_multi(
  gp_layer(
    x = df$wt,
    y = df$mpg,
    style = "points",
    title = "cars",
    col = 4,
    pch = 7,
    cex = 1.2
  ),
  gp_layer(
    data = trend,
    style = "lines",
    title = "linear fit",
    col = 2,
    lwd = 2,
    lty = 1
  ),
  main = "MPG vs weight",
  subtitle = "Points plus fitted line",
  xlab = "Weight",
  ylab = "MPG",
  legend_position = "top right",
  legend_box = TRUE,
  grid_col = 8,
  grid_lty = 2
)
```

## 主题对象

如果很多图共用同一套风格，可以先创建 `gp_theme()`。

```r
paper_theme <- gp_theme(
  font_family = "Arial",
  title_size = 16,
  subtitle_size = 10,
  axis_size = 12,
  tick_size = 10,
  legend_size = 10,
  legend_position = "outside right top",
  legend_box = TRUE,
  grid_col = "gray80",
  grid_lty = 2,
  border_lwd = 1,
  border_col = "gray30",
  background = "white"
)

gp_line(
  x, sin(x),
  col = 2,
  lwd = 2,
  main = "Sine wave",
  subtitle = "Shared theme",
  xlab = "x",
  ylab = "sin(x)",
  legend = "sin(x)",
  theme = paper_theme
)
```

## 柱状图和直方图

```r
gp_bar(
  c(A = 3, B = 7, C = 5),
  fill = 0.65,
  col = 3,
  lwd = 1.2,
  main = "Bar plot",
  theme = paper_theme
)

gp_histogram(
  rnorm(1000),
  bins = 30,
  fill = 0.55,
  col = 4,
  lwd = 1,
  main = "Histogram",
  theme = paper_theme
)
```

## 三维图

```r
f <- function(x, y) sin(sqrt(x^2 + y^2))

gp_surface(
  f,
  type = "lines",
  col = 4,
  lwd = 1,
  main = "3D surface",
  subtitle = "Styled from R",
  xlab = "x",
  ylab = "y",
  zlab = "z",
  theme = paper_theme
)
```

## 保存图片

```r
gp_line(
  x, sin(x),
  col = 2,
  lwd = 2,
  output = "figures/sine.png",
  terminal = "pngcairo size 1200,800",
  main = "Sine wave"
)
```

如果只想保存、不弹窗：

```r
gp_line(
  x, sin(x),
  col = 2,
  lwd = 2,
  output = "figures/sine.png",
  preview = FALSE
)
```
