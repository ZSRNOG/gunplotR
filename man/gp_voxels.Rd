% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots3d_advanced.R
\name{gp_voxels}
\alias{gp_voxels}
\title{3D Voxel Cloud}
\usage{
gp_voxels(
  x,
  y = NULL,
  z = NULL,
  value = NULL,
  type = c("points", "boxes"),
  ...
)
}
\arguments{
\item{x}{Numeric x values or a data frame with x/y/z/value columns.}

\item{y, z}{Optional numeric coordinates when \code{x} is not a data frame.}

\item{value}{Optional scalar values used for palette coloring.}

\item{type}{\code{"points"} for point voxels or \code{"boxes"} for box voxels.}

\item{...}{Additional arguments passed to \code{gp_points4d()} or \code{gp_boxes3d()}.}
}
\value{
Invisibly returns rendering details from the plotting process.
}
\description{
Draw voxel centers as a 4D point cloud. This is useful for inspecting a
volumetric scalar field before extracting an isosurface.
}
\examples{
\dontrun{
grid <- expand.grid(x = seq(-1, 1, length.out = 8),
                    y = seq(-1, 1, length.out = 8),
                    z = seq(-1, 1, length.out = 8))
value <- with(grid, x^2 + y^2 + z^2)
gp_voxels(grid$x, grid$y, grid$z, value, cex = 0.8,
          cblabel = "radius^2", main = "Voxel cloud")
}
}
